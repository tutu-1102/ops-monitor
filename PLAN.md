# 运维监控项目 — 方案文档

> 用途：给项目作者看的设计说明。每个里程碑做完后用自己的话复述一遍，就是面试素材。

## 1. 项目定位

一句话：**一台服务器上的"体检仪 + 报警器"**。
- 体检仪：定时采集 CPU、内存、磁盘、网络指标
- 报警器：指标超过阈值时，按级别通知管理员

## 2. 技术选型（面试常问"为什么"）

| 选型 | 理由 |
|------|------|
| Python + psutil | 运维脚本生态最主流；psutil 是跨平台系统信息标准库 |
| Flask | 轻量、学习成本低；本项目 API 简单，不需要 Django 的复杂度 |
| SQLite | 单机部署零依赖；历史数据量小（分钟级）足够 |
| 采集模式：push（本机采集直写） | 单机监控最简方案；多机监控时才需要 pull + 中心采集（Prometheus 思路，作为扩展话题） |
| 存储：指标原始值而非平均值 | 保留原始数据，告警与图表都基于真实数据 |

## 3. 功能设计

### 3.1 采集器（collector.py）
- 指标：CPU 使用率（1 秒采样取均值）、内存使用率、磁盘各分区使用率、网络收发字节速率
- 采集周期：默认 5 秒（配置可调）——周期权衡：太短增加负载，太长漏掉瞬时尖峰

### 3.2 存储（storage.py）
- SQLite 单表 `metrics(timestamp, cpu_percent, mem_percent, disks_json, net_in_bytes, net_out_bytes)`
- 只保留最近 7 天数据（配置可调），自动清理
- 表结构演进用 `PRAGMA table_info` + `ALTER TABLE ADD COLUMN` 做轻量迁移（里程碑 2 加了内存已用/总量 GB）

### 3.3 可视化（里程碑 2，app.py + static/）
- 本地 ECharts（离线可用）+ 深色监控风单页仪表盘
- 实时卡片（3 秒轮询）+ 时间范围切换（实时/1h/24h/7d，范围越大刷新越慢）
- **服务端抽稀聚合**：7 天分钟级约 12 万条原始数据，按时间桶取均值压缩到 300 个点再下发，前端不卡顿（面试点）

### 3.3 告警（里程碑 3）
- 三级：P0（CPU>95% 持续 2 次采样）、P1（内存>90%）、P2（磁盘>85%）
- 通知渠道：钉钉机器人 Webhook + SMTP 邮件
- 降噪：同指标持续告警只发首条 + 恢复时发一条恢复通知

### 3.4 实战细节（里程碑 4，面试差异化点）
- CPU 高负载时自动抓取 top 进程快照
- 磁盘快满时自动清理过期日志
- 压测脚本（人为打满 CPU/磁盘，验证告警链路）

## 4. 部署方案（里程碑 5）
- Dockerfile + docker-compose（应用 + 数据卷）
- 云服务器：轻量应用服务器（香港节点免备案），简历放真实链接

## 5. 面试预演问题清单（做完项目后过一遍）

1. 为什么采集周期选 5 秒？太短/太长会怎样？
2. 采集到的 CPU 使用率是瞬时值吗？为什么采样 1 秒取均值？
3. 告警为什么分级？怎么防止告警刷屏？
4. SQLite 存历史数据有什么限制？数据量大后换什么？
5. 单机监控和多机监控架构有什么区别？（引出 Prometheus/Exporter 话题）
6. 部署时遇到过什么坑？怎么排查的？
7. 如果监控程序自己挂了，谁来监控监控？（引出 supervisor/systemd 守护话题）
